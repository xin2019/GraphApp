
#include<graphics.h>
#include <iostream> 
#include<stdio.h>
#include"book_main_interface.h"
using namespace std;

int main(){
book_main_interface();
/*	initgraph(720,540);
	mouse_msg msg = {0};
				for(; is_run(); delay_fps(1000))
				{
					
					
					while (mousemsg())
					{
						msg = getmouse();
					}
					cleardevice();
					setcolor(WHITE);
					xyprintf(0, 0, "x = %10d  y = %10d",
						msg.x, msg.y);	
				}
*/

				return 0;
}